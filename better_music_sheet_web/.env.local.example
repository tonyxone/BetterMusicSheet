# Copy to .env.local for local dev (gitignored).
#
# Point this at your local backend (`.venv\Scripts\python.exe server.py`,
# see the repo root README) - use the production URL only when building for
# a real deployment.
NEXT_PUBLIC_API_BASE=http://localhost:8000

# Cognito sign-in. OPTIONAL: leave both unset and the app still works
# end-to-end as a guest - the Sign in button simply doesn't render (see
# lib/auth.ts). Set them to exercise the real hosted-UI flow locally, using
# ../infra's `terraform output` values, and make sure
# http://localhost:3000/auth/callback/ is one of the app client's registered
# callback URLs (infra/cognito.tf registers it alongside the production one).
#
# NEXT_PUBLIC_COGNITO_DOMAIN has no trailing slash, e.g.
# https://better-music-sheet.auth.us-west-1.amazoncognito.com
NEXT_PUBLIC_COGNITO_DOMAIN=
NEXT_PUBLIC_COGNITO_CLIENT_ID=
