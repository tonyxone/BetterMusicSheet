// The canonical origin, used by robots.ts and sitemap.ts. Both are generated
// at build time into a static export, so this cannot be read from a request.
export const SITE_URL = "https://bettermusicsheet.com";
